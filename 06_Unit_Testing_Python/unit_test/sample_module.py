# Add two numbers.
def add2num(x, y):
    return x + y

# Return x to the power of y.
def pow2num(x, y):
    """Returns x power of y.
    >>> pow2num(2, 6)
    64
    >>> pow2num(10, -3)
    0.001
    """
    return x**y